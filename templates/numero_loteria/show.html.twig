{% extends 'base.html.twig' %}

{% block title %}NumeroLoteria{% endblock %}

{% block body %}
    <h1>NumeroLoteria</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ numero_loterium.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ numero_loterium.number }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ numero_loterium.price }}</td>
            </tr>
            <tr>
                <th>FechaLimite</th>
                <td>{{ numero_loterium.fechaLimite ? numero_loterium.fechaLimite|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ numero_loterium.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_numero_loteria_index') }}">back to list</a>

    <a href="{{ path('app_numero_loteria_edit', {'id': numero_loterium.id}) }}">edit</a>

    {{ include('numero_loteria/_delete_form.html.twig') }}
{% endblock %}
